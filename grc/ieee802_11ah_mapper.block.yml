# auto-generated by grc.converter

id: ieee802_11ah_mapper
label: HaLow Mapper
category: '[IEEE802.11AH]'

parameters:
-   id: encoding
    label: Encoding
    dtype: raw
    default: ieee802_11ah.BPSK_1_2
-   id: debug
    label: Debug
    dtype: bool
    default: 'False'
    options: ['True', 'False']
    option_labels: [Enable, Disable]

inputs:
-   domain: message
    id: in

outputs:
-   domain: stream
    dtype: byte
    multiplicity: '1'

templates:
    imports: import ieee802_11ah
    make: ieee802_11ah.mapper(${encoding}, ${debug})
    callbacks:
    - set_encoding(${encoding})

file_format: 1
